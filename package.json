{
  "name": "leap-app",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "build": "cd frontend && bun run build",
    "start": "cd frontend && bunx vite preview --host --port $PORT"
  }
}

